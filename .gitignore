# Secrets — never commit
.env
.env.*
# ...except the template, which carries no values. The `.env.*` rule above was
# silently swallowing it, which is why the README referenced a file that was
# never in the repo.
!.env.example

# Node
node_modules/

# Cached provider responses (safe to delete; costs API calls to rebuild)
.cache/

# Generated output (regenerated by `npm run build`)
rankings.html
audit.json
